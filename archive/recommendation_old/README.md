# Archived Recommendation Scripts

These files are legacy analysis helpers and are not part of the final report pipeline.

The report uses `src/run_recommendation_experiment.py` as the single entry point.

Legacy scripts still present in `src/` are documented in `recommendation_inventory.md` so they are easy to identify during maintenance. Do not use them to regenerate report outputs unless the workflow is explicitly being revised.
